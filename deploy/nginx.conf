server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    location /goim/ {
        proxy_pass http://logic:3111;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
